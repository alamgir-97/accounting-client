import Modal from "../components/ui/Modal";


const AddTaskModal2 = ({isOpen, setIsOpen}) => {
    return (
        <Modal isOpen={isOpen} setIsOpen={setIsOpen} title={'Programming Hero2'}>lorem2202   </Modal>
    );
};

export default AddTaskModal2;